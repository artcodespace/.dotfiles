IDEAS

- not sure how to square the naming, but basically from a fuzzy picker of the tasks:
  - if a session/worktree exists, connect to it
  - if not, create it
- when work complete, run a script to translate branch name into pr with title:
  <conventional-commit-type>(rvr-<linear task id>): free description

- ultimately want to manage all of this with nix and direnv, away from mise/local scripts
